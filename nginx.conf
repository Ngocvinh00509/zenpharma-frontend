server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cấu hình API Proxy linh hoạt (không làm crash Nginx khi thiếu Backend)
    # location /api/ {
    #     # DNS resolver nội bộ của Docker
    #     resolver 127.0.0.11 valid=30s ipv6=off;
        
    #     # Gán URL vào biến để Nginx kiểm tra dynamic runtime thay vì bắt buộc có sẵn lúc khởi động
    #     set $backend_target http://api-gateway:8080;

    #     proxy_pass $backend_target;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # Gzip Compression
    gzip on;
    gzip_types text/plain application/javascript text/css application/json image/svg+xml;
    gzip_min_length 1000;

    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";
}
